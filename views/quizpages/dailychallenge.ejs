<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Daily Challenges</title>
  <link rel="shortcut icon" href="/logo.png" type="image/x-icon" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"/>
  <style>
    :root{
      --primary:#7B16FF;
      --primary-trans:rgba(123,22,255,0.4);
      --surface:#16171A;
      --dark:#101010;
      --white:#f5f5f5;
    }

    *{ box-sizing:border-box; }

    body{
      margin:0;
      font-family:Arial,sans-serif;
      background:#101010;
      color:var(--white);
      min-height:80vh;
      display:flex;
      flex-direction:column;
      align-items:center;
    }

    .navbar{
      background:var(--surface);
      width:99%;
      padding:1rem 2rem;
      display:flex;
      justify-content:space-between;
      align-items:center;
      box-shadow:0 4px 10px rgba(0,0,0,0.2);
    }
    .navbar h2{ color:var(--primary); margin:0; }
    .navbar a{ color:var(--white); text-decoration:none; margin-left:1rem; }
    .navbar a:hover{ color:var(--primary); }

    .container{
      width:100%;
      max-width:600px;
      padding:2rem 1rem;
      display:flex;
      flex-direction:column;
      align-items:center;
      gap:2rem;
    }

    .card{
      background:var(--surface);
      border-radius:15px;
      padding:2rem;
      width:100%;
      max-width:400px; /* max width for cards */
      box-shadow:0 4px 15px rgba(0,0,0,0.4);
      text-align:center;
      transition:transform .2s ease;
    }
    .card:hover{ transform:translateY(-5px); }

    .card-img{
      width:100px;
      height:100px;
      margin:0 auto 1rem auto;
      object-fit:contain;
      background:rgba(255,255,255,0.05);
      border-radius:50%;
      padding:10px;
      display:flex;
      align-items:center;
      justify-content:center;
    }

    .card h2{ margin:1rem 0; color:var(--primary); }
    .card p{ font-size:16px; margin-bottom:1.5rem; color:#ddd; }

    .play-btn{
      background:var(--primary);
      color:var(--white);
      padding:12px 24px;
      font-size: 17px;
      border:none;
      border-radius:8px;
      font-weight:bold;
      cursor:pointer;
      transition:all .2s ease;
    }
    .play-btn:hover{ background:var(--primary-trans); color:var(--primary); }
    .play-btn i{ margin-right:8px; }

    @media(max-width:400px){
      .card{ padding:1.5rem; }
      .card-img{ width:100px; height:100px; }
      .play-btn{ width:75%; }
    }
  </style>
</head>

<body>
  <div class="navbar">
    <h2>QUIZIFY</h2>
    <% if(currentUser){ %>
      <div>
        <a href="/"><i class="fa-solid fa-house"></i> Home</a>
        <a href="/logout"><i class="fa-solid fa-right-from-bracket"></i> Logout</a>
      </div>
    <% } else { %>
      <div>
        <a href="/login"><i class="fa fa-sign-in"></i> Login</a>
        <a href="/register"><i class="fa fa-user-plus"></i> Register</a>
      </div>
    <% } %>
  </div>

  <div class="container">
    <div class="card">
      <img src="https://res.cloudinary.com/di5q8uqqc/image/upload/v1756146015/quiz_qoaoxf.jpg" alt="Quiz Icon" class="card-img">
      <h2>Daily Quiz</h2>
      <p>Test your knowledge with today's set of quiz questions!</p>
      <a href="/show"><button class="play-btn"><i class="fa-solid fa-question-circle"></i> Play Quiz</button></a>
    </div>

    <div class="card">
      <img src="https://res.cloudinary.com/di5q8uqqc/image/upload/v1756146004/sudoku_hyst00.png" alt="Sudoku Icon" class="card-img">
      <h2>Mini Sudoku</h2>
      <p>Solve today's 6x6 Sudoku puzzle and challenge your logic!</p>
      <a href="/sudoku"><button class="play-btn"><i class="fa-solid fa-th"></i> Play Sudoku</button></a>
    </div>
  </div>
</body>
</html>