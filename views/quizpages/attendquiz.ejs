<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ENTER CODE</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
    :root {
    --black: #000000;
    --white: #ffffff;
    --darkest: #101010;
    --darker: #16171A;
    --dark: #A3AFBF;
    --primary: #7B16FF;
    --primary-trans: rgba(123, 22, 255, 0.4);
    --bg: var(--darkest);
    --color: var(--white);
    --surface: var(--darker);
}

body {
    background: var(--bg);
    color: var(--color);
    font-family: 'Arial', sans-serif;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 80vh;
    margin: 0;
    text-align: center;
}

    form {
    background: var(--surface);
    padding: 20px;
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    gap: 10px;
    width: 300px;
}

input,
button {
    padding: 10px;
    border: none;
    border-radius: 5px;
}


button {
    background: var(--primary);
    color: var(--white);
    cursor: pointer;
}


h1,
h2 {
    color: var(--white);
    text-align: center;
}


label {
    display: block;
    text-align: left;
    margin-top: 1rem;
    font-size: 1rem;
    color: var(--dark);
}


input {
    padding: 10px;
    margin-top: 5px;
    border: 1px solid var(--dark);
    border-radius: 5px;
    background: var(--darker);
    color: var(--white);
    font-size: 1rem;
    transition: 0.3s;
    outline: none;
}

input:hover,
input:focus {
    border-color: var(--primary);
    box-shadow: 0 0 8px var(--primary-trans);
}

        .navbar {
    position: fixed;
    top: 0;
    width: 95%;
    background: var(--surface);
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    z-index: 1000;
}


        .navbar h2 {
            color: var(--primary);
            margin: 0;
        }

        .navbar a {
            color: var(--white);
            text-decoration: none;
            margin-left: 1rem;
            transition: color 0.3s;
        }

        .navbar a:hover {
            color: var(--primary);
        }
    </style>
</head>
<body>
    <div class="navbar">
        <h2>QUIZIFY</h2>
        <div>
            <a href="/"><i class="fa-solid fa-house"></i> Home</a>
            <a href="/logout"><i class="fa-solid fa-right-from-bracket"></i> Logout</a>
        </div>
    </div>
    <form action="/attendquiz" method="POST">
    <label for="quizcode"> <h2>Enter Quiz Code : </h2></label>
    <input type="text" id="quizcode" name="quizcode" autofocus required>
    <button type="submit">ATTEND QUIZ</button>
    </form>
</body>
</html>